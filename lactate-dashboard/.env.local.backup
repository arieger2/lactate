# Environment Configuration for Lactate Dashboard
# NOTE: Database configuration is also synced to config/app.config.json for Settings UI

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=laktat
DB_USER=postgres
DB_PASSWORD=LisgumuM20251!

# Application Configuration
NODE_ENV=development
PORT=3000

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here

# Optional: Database SSL  
DB_SSL=false

# Optional: Connection Pool Settings
DB_POOL_MIN=2
DB_POOL_MAX=10