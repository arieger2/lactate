# Database Configuration
# IMPORTANT: Copy this file to .env and change the password!
POSTGRES_DB=laktat
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_secure_password_here

# Application Configuration
NODE_ENV=production
APP_PORT=3000

# SSL for database connection (set to true if using remote DB with SSL)
DB_SSL=false
